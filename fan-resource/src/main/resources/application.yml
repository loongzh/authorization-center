server:
  port: 10501
spring:
  profiles:
    active: dev
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://127.0.0.1:10301
management:
  endpoints:
    web:
      exposure:
        include: "*"
